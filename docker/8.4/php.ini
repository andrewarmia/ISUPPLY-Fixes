[PHP]
post_max_size = 100M
upload_max_filesize = 100M
variables_order = EGPCS
memory_limit = 512M
max_execution_time = 300
max_input_vars = 3000

file_uploads = On

allow_url_fopen = Off ; Security: prevent remote file inclusion attacks

display_errors = Off ; Security: don't show errors to users

log_errors = On ; Security: log errors for debugging

error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT ; Security: report errors but not deprecation warnings

max_execution_time = 300

max_input_vars = 3000

post_max_size = 100M

upload_max_filesize = 100M

variables_order = EGPCS

memory_limit = 512M

file_uploads = On

allow_url_fopen = Off

display_errors = Off

log_errors = On

error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT